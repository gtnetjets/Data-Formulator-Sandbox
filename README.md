# Data-Formulator-Sandbox

This sandbox contains a tiny transformation pipeline helper that can be used to
compose data shaping callables.  The accompanying tests demonstrate the bugfix
for ensuring that individual pipeline instances no longer share step state by
default.
